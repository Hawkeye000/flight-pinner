<div class="row">
  <div class="small-12">
    <h1>Airlines</h1>
    <%= paginate @airlines %>
    <table>
      <thead>
        <th>Name</th>
        <th>Country</th>
        <th>IATA</th>
      </thead>
      <tbody>
        <% @airlines.each do |airline| %>
          <tr>
            <td><%= airline.name %></td>
            <td><%= airline.country %></td>
            <td><%= airline.iata %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= paginate @airlines %>
  </div>
</div>
